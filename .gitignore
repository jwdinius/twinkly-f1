# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# uv / venv
.venv/

# Rendered output
out/

# Large derived mosaic rasters — regenerable via scripts/build_*_mosaic.sh.
# The sidecars (configs/*_mosaic.yaml) ARE tracked; the PNGs they point at are
# build output (~40-55 MB) and live outside the repo. Same for the GDAL stats
# sidecar emitted next to them.
configs/monaco_mosaic.png
configs/silverstone_mosaic.png
configs/*_mosaic.png.aux.xml
