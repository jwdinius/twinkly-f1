# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# uv / venv
.venv/

# Rendered output
out/

# Large derived mosaic raster — regenerable via scripts/build_monaco_mosaic.sh.
# The sidecar (configs/monaco_mosaic.yaml) IS tracked; the PNG it points at is
# build output (~42 MB) and lives outside the repo.
configs/monaco_mosaic.png
