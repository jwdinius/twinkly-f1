# twinkly-f1

LEGO Technic MCL39 + Twinkly Squares wall display: mockup, video preview, and hardware-push pipeline for a top-down satellite-imagery F1 lap visualization.

## Agent skills

### Issue tracker

Issues and PRDs live in GitHub Issues at `jwdinius/twinkly-f1`. See `docs/agents/issue-tracker.md`.

### Triage labels

Default vocabulary — each role's label string equals its name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` + one `docs/adr/` at the repo root. See `docs/agents/domain.md`.
